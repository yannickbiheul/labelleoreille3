<div class="container d-flex justify-content-center align-items-center" id="presentation">
	<div class="row">
		<div class="col-lg-6 d-flex justify-content-end">
            <img class="photoProfil" src="/photos/{{ general.photo }}" alt="photo">
        </div>
        <div class="col-lg-6 d-flex justify-content-start">
            <div class="containerPresentation d-flex flex-column justify-content-center align-items-center">
                <em>{{ general.citation }}</em>
                <em>{{ general.proprietaire }}</em>
                <hr>
                <h3>{{ general.description }}</h3>
            </div>
        </div>
	</div>
</div>

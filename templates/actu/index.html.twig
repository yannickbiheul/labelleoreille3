{% extends 'base.html.twig' %}

{% block title %}La Belle Oreille | Actualités
{% endblock %}

{% block body %}
	<div class="container" id="actus">
		{% for actu in actus %}
			<div class="row d-flex flex-column justify-content-center align-items-center">
				<div class="col-lg-6">
					<h2>{{ actu.titre }}</h2>
				</div>
				<div class="col-lg-6">
					<a href="/images/{{ actu.image.nom }}"><img src="/images/{{ actu.image.nom }}" alt="image" class="img-fluid"></a>
				</div>
			</div>
		{% endfor %}
	</div>
{% endblock %}
